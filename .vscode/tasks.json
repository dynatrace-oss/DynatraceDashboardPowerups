{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "terser",
            "type": "shell",
            "command": "for i in `ls *.js |grep -v min.js | sed -e 's/\\.js//'`; do terser $i.js -o $i.min.js --source-map \"url='file:///Users/lucas.hocker/Projects/DynatraceDashboardPowerup/$i.min.js.map'\"; done",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "reveal": "never"
              }
        }
    ]
}