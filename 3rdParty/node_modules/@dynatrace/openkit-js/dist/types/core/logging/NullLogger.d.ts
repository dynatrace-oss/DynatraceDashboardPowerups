import { Logger } from '../../api';
/**
 * Logger implementation to discard all messages.
 *
 * @see {@link defaultNullLogger}
 */
export declare class NullLogger implements Logger {
    debug(...msg: any[]): void;
    info(...msg: any[]): void;
    warn(...msg: any[]): void;
    error(...msg: any[]): void;
}
export declare const defaultNullLogger: NullLogger;
//# sourceMappingURL=NullLogger.d.ts.map