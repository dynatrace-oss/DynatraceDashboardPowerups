import { Logger, LoggerFactory } from '../../api';
/**
 * LoggerFactory implementation to create loggers which discard all messages.
 *
 * @see {@link defaultNullLoggerFactory}
 */
export declare class NullLoggerFactory implements LoggerFactory {
    createLogger(name: string): Logger;
}
export declare const defaultNullLoggerFactory: NullLoggerFactory;
//# sourceMappingURL=NullLoggerFactory.d.ts.map