import { CommunicationChannel, LoggerFactory, StatusRequest, StatusResponse } from '../../../../api';
import { HttpClient } from '../HttpClient';
export declare class HttpCommunicationChannel implements CommunicationChannel {
    private readonly context;
    private readonly logger;
    private state;
    constructor(client: HttpClient, loggerFactory: LoggerFactory);
    sendNewSessionRequest(url: string, request: StatusRequest): Promise<StatusResponse>;
    sendPayloadData(url: string, request: StatusRequest, query: string): Promise<StatusResponse>;
    sendStatusRequest(url: string, request: StatusRequest): Promise<StatusResponse>;
    setNextState(channel: CommunicationChannel): void;
}
//# sourceMappingURL=HttpCommunicationChannel.d.ts.map